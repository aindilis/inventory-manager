(use google to find all search results for the barcode, and
 figure out if any of them have all results, and then scrape that site)

(extract inventory automatically from receipts that have been scanned in)
